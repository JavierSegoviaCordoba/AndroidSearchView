package com.javiersc.materialsearchview.utils
/*

import android.view.View
import com.javiersc.materialsearchview.view.materialsearchview.MaterialSearchView
import kotlinx.android.synthetic.main.menu_msv.view.*

internal var keyboardMargin: Float = 0f
internal var fixTranslationY: Int = 0

internal fun fixRecyclerViewHeight(materialSearchView: MaterialSearchView) {
    materialSearchView.recyclerViewSuggestions.addOnLayoutChangeListener(object : View.OnLayoutChangeListener {
        override fun onLayoutChange(
            v: View, left: Int, top: Int, right: Int, bottom: Int,
            oldLeft: Int, oldTop: Int,
            oldRight: Int, oldBottom: Int
        ) {
            val item = materialSearchView.recyclerViewSuggestions.getChildAt(0)
            val itemHeight = item?.height ?: 0
            materialSearchView.constrainLayoutParent
                .setPadding(0, 0, 0, itemHeight + keyboardMargin.toInt() + fixTranslationY)
            v.removeOnLayoutChangeListener(this)
        }
    })
}*/
