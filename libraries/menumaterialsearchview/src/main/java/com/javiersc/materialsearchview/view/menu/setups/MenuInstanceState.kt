package com.javiersc.materialsearchview.view.menu.setups
/*

import com.javiersc.materialsearchview.view.menu.MenuMaterialSearchView
import com.javiersc.materialsearchview.view.menu.utils.MenuState
import kotlinx.android.synthetic.main.menu_msv.view.*

internal fun setupRestoreInstanceState(state: MenuState, menuMaterialSearchView: MenuMaterialSearchView) =
    with(menuMaterialSearchView) {
        isOpen = state.isOpen
        if (isOpen) {
            cardViewBackground.visible()
            cardViewSearch.visible()
            cardViewSuggestion.visible()
        } else {
            cardViewBackground.invisible()
            cardViewSearch.invisible()
            cardViewSuggestion.invisible()
        }
    }*/
